const router = require('express').Router();
// Import User model
const { User } = require('../../models');

// TODO: Add user routes

// POST (create user /) - request to create a user and log in to newly created account

// POST (/login) - request to log into existing account

// POST (/logout) - request to log out of account